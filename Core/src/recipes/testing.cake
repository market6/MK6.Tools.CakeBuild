///////////////////////////////////////////////////////////////////////////////
// TOOLS
///////////////////////////////////////////////////////////////////////////////



///////////////////////////////////////////////////////////////////////////////
// TASK DEFINITIONS
///////////////////////////////////////////////////////////////////////////////

Task("Test-VSTest")
    .Does(() =>
{
    var testsPattern = parameters.Paths.Directories.Source.FullPath + "/**/*Tests.dll";
    Verbose("testsPattern: {0}", testsPattern);
    VSTest(testsPattern, new VSTestSettings().WithVisualStudioLogger());
});

Task("Test")
    .IsDependentOn("Test-VSTest")
    .Does(() =>
{
});